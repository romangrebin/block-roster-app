# Data Model Draft (v0) — superseded

Original schema draft, before the `addresses`→`residences` rework. No longer accurate — see `minimal-schema-proposal.md` for the current (implemented) schema. Kept only for historical rationale on the original geometry-required design, if that's ever useful.
